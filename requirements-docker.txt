# Subset of requirements.txt for the FastAPI container image.
# Excludes CUDA wheels (nvidia-*, triton), the GPU torch wheel (installed
# separately from the CPU index in the Dockerfile), and the heavy 3D viz
# stack (pyvista, vtk, imageio, av) which is only used by visualization.py.
# matplotlib IS kept because convergence_tracker.py imports it at module
# load — Agg backend is selected via MPLBACKEND=Agg in the Dockerfile, so
# no system GUI libs are required.

# Build-time
cython==3.1.2

# Core
numpy==2.2.6
trimesh==4.6.13
rtree==1.4.0
networkx==3.4.2
pyyaml==6.0.2
pillow==11.3.0
# COLLADA loader for trimesh: required for .dae mesh imports (some
# robot URDFs reference .dae collisions; without it trimesh.load
# raises ImportError at first DAE encounter).
pycollada==0.8

# Plotting (headless via MPLBACKEND=Agg) — required because
# convergence_tracker.py imports matplotlib at module load.
matplotlib==3.10.3
contourpy==1.3.2
cycler==0.12.1
fonttools==4.58.5
kiwisolver==1.4.8
pyparsing==3.2.3

# Torch transitive deps (torch itself is installed in the Dockerfile)
filelock==3.18.0
fsspec==2025.5.1
jinja2==3.1.6
markupsafe==3.0.2
mpmath==1.3.0
sympy==1.14.0
typing-extensions==4.14.1
psutil==7.0.0

# Misc transitive
certifi==2025.6.15
charset-normalizer==3.4.2
idna==3.10
packaging==25.0
platformdirs==4.3.8
python-dateutil==2.9.0.post0
requests==2.32.4
setuptools==80.9.0
six==1.17.0
urllib3==2.5.0

# Web layer
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
